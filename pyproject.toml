[project]
name = "joblex"
version = "0.1.0"
description = "JOBLEX Academia-Industry Collaboration Portal"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0.0",
    "flask-cors>=4.0.0",
    "requests>=2.31.0",
]

[tool.vercel]
entrypoint = "backend.app:app"